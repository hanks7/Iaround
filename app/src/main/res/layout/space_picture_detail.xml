<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/all_backgroud_color"
    android:orientation="vertical" >

    <net.iaround.ui.comon.JazzyViewPager
        android:id="@+id/pager"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <TextView
        android:id="@+id/right_text_num"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_marginRight="9dp"
        android:layout_marginTop="9dp"
        android:ellipsize="end"
        android:gravity="center"
        android:padding="5dip"
        android:singleLine="true"
        android:textColor="@color/black"
        android:textSize="20sp" />

      <RelativeLayout
        android:id="@+id/ly_del_banner"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:background="@drawable/gray_trains_pressed_black"
        android:visibility="gone"
        android:layout_alignParentBottom="true" >

        <ImageView
            android:id="@+id/iv_del_picture"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:gravity="center"
            android:padding="5dip"
            android:src="@drawable/chatgame_del_0" />
    </RelativeLayout>

</RelativeLayout>